<% 
  require 'digest/md5'
  realm = @www_auth_realm
  @www_users.each do |username, password|
    encoded_password = Digest::MD5.hexdigest(username + ':' + realm + ':' + password)
-%>
<%= "#{username}:#{realm}:#{encoded_password}"  %>
<% end -%>