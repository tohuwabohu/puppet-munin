<% 
  require 'digest/sha1'
  require 'base64' 
  @www_authorized_users.each do |username, password|
    encoded_password = Base64.encode64(Digest::SHA1.digest(password))
-%>
<%= "#{username}:{SHA}#{encoded_password}" -%>
<% end -%>